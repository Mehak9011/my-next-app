# Copy this file to `.env.local` and fill in your values.
# Never commit real secrets — only this example file is safe to commit.

# App name shown in the browser/UI (must be prefixed NEXT_PUBLIC_ to be visible on the client)
NEXT_PUBLIC_APP_NAME=my-next-app

# Example values — replace with your own variables as needed
DATABASE_URL=
SECRET_KEY=